# Vocabularies, encodings, and other cross-cutting behavior.
name: misc
dir: fixtures

cases:
  - name: vocab
    dir: vocab/Basic
    args: .
    exit: 1
    want: |
      test.md:3:5:Vale.Avoid:Avoid using 'Mac OS X'.
      test.md:7:1:Vale.Terms:Use 'definately' instead of 'Definately'.
      test.md:13:1:Vale.Terms:Use 'Documentarians' instead of 'documentarians'.
      test.md:17:11:Vale.Terms:Use 'Log4j' instead of 'log4j'.
      test.md:19:1:Vale.Terms:Use 'ABCDEF' instead of 'ABCDEf'.
      test.md:21:1:Vale.Terms:Use 'ABC-DEF' instead of 'ABC-DEf'.
      test.md:23:1:Vale.Terms:Use 'PLuG' instead of 'plug'.
      test.md:27:17:Vale.Terms:Use '[Oo]bservability' instead of 'oBservability'.
      test.md:31:25:Vale.Terms:Use '[Oo]bservability' instead of 'oBservability'.
      test.md:32:8:Vale.Terms:Use '[Oo]bservability' instead of 'oBservability'.

  - name: vocab-nonword
    about: "#1058"
    dir: vocab/Nonword
    args: .
    exit: 0
    want: |
      test.md:1:18:Bug.Quotes:Commas and periods go inside quotation marks.
      test.md:3:27:Bug.Quotes:Commas and periods go inside quotation marks.

  - name: vocab-multiple
    dir: vocab/Multi
    args: .
    exit: 1
    want: |
      test.md:3:5:Vale.Avoid:Avoid using 'Mac OS X'.
      test.md:5:6:Vale.Terms:Use 'Definately' instead of 'definately'.
      test.md:7:29:Vale.Terms:Use 'IDEA' instead of 'idea'.
      test.md:13:1:Vale.Terms:Use 'Documentarians' instead of 'documentarians'.
      test.md:15:5:Vale.Terms:Use 'MyProduct' instead of 'Myproduct'.
      test.md:17:5:Vale.Terms:Use 'MyProduct Enterprise' instead of 'Myproduct enterprise'.
      test.md:19:5:Vale.Terms:Use 'MyProduct Enterprise' instead of 'MyProduct enterprise'.

  - name: vocab-phrases
    about: "#1035"
    dir: vocab/Phrases
    args: .
    exit: 1
    want: |
      test.md:3:8:Test.Place:Avoid 'place'.
      test.md:5:3:Vale.Spelling:Did you really mean 'flooberg'?

  - name: line-endings
    dir: misc/line-endings
    args: .
    exit: 0
    want: |
      CR.md:3:11:vale.Editorializing:Consider removing 'very'
      CR.md:32:11:vale.Editorializing:Consider removing 'very'
      CRLF.md:3:11:vale.Editorializing:Consider removing 'very'
      CRLF.md:32:11:vale.Editorializing:Consider removing 'very'

  - name: attributes
    dir: misc/ids
    args: .
    requires: asciidoctor
    exit: 0
    want: |
      test.adoc:7:1:Markup.ID:Use 'leader' rather than 'Master'.
      test.adoc:12:1:Markup.ID:Use 'leader' rather than 'master'.
      test.adoc:14:55:Markup.ID:Use 'leader' rather than 'master'.
      test2.adoc:5:26:Markup.ID:Use 'leader' rather than 'master'.
      test2.adoc:5:146:Markup.ID:Use 'leader' rather than 'master'.

  - name: unicode
    dir: misc/unicode
    args: .
    requires: rst2html
    exit: 0
    want: |
      test.py:2:27:vale.Hedging:Consider removing 'probably'
      test.rst:1:36:vale.Annotations:'TODO' left in text
      test.rst:2:55:vale.Hedging:Consider removing 'probably'
      test.rst:4:23:vale.Editorializing:Consider removing 'very'
      test.txt:1:36:vale.Annotations:'TODO' left in text
      test.txt:2:55:vale.Hedging:Consider removing 'probably'
      test.txt:4:23:vale.Editorializing:Consider removing 'very'

  - name: duplicate-matches
    dir: misc/duplicates
    args: .
    requires: asciidoctor
    exit: 1
    want: |
      test.adoc:1:3:Vale.Spelling:Did you really mean 'hav'?
      test.adoc:1:21:write-good.E-Prime:Avoid using "that's"
      test.adoc:4:3:Vale.Spelling:Did you really mean 'hav'?
      test.adoc:4:21:write-good.E-Prime:Avoid using "that's"
      test.md:1:6:write-good.E-Prime:Avoid using "is"
      test.md:1:9:vale.Editorializing:Consider removing 'very'
      test.md:1:15:vale.Editorializing:Consider removing 'very'
      test.md:1:39:vale.Editorializing:Consider removing 'very'
      test.md:1:57:vale.Editorializing:Consider removing 'very'
      test.md:3:14:write-good.E-Prime:Avoid using "are"
      test.md:3:28:write-good.E-Prime:Avoid using "be"
      test.md:5:9:write-good.E-Prime:Avoid using "is"
      test.md:5:95:Vale.Spelling:Did you really mean 'multimicrophone'?
      test.md:5:193:demo.CommasPerSentence:More than 3 commas!
      test2.md:1:6:write-good.E-Prime:Avoid using "is"
      test2.md:2:24:demo.Fancy:Change
      test2.md:2:40:write-good.E-Prime:Avoid using "there's"

  - name: issue-1007
    about: "#1007 -- a term repeated later in the frontmatter or a blockquote
      doesn't pull the alert off its first occurrence, and every occurrence
      is reported"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion
        Vocab = test-vocab

        [*.md]
        BasedOnStyles = Vale
      styles/config/vocabularies/test-vocab/accept.txt: |
        CASTLE
      front.md: |
        ---
        title: castle
        tags: ['castle', 'gitlab', 'sonarqube']
        weight: 8
        ---

        ## Intro

        Hello, I love a CASTLE.
      quote.md: |
        > Hello, I love a castle.
        > It's castle!
    args: front.md quote.md
    exit: 1
    want: |
      front.md:2:8:Vale.Terms:Use 'CASTLE' instead of 'castle'.
      quote.md:1:19:Vale.Terms:Use 'CASTLE' instead of 'castle'.
      quote.md:2:8:Vale.Terms:Use 'CASTLE' instead of 'castle'.

  - name: issue-687
    about: "#687 -- spans cover the file's actual bytes: `&rsquo;` is
      normalized for matching, but the reported span accounts for its width,
      whether the entity sits inside the match or before it"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.html]
        T.Contraction = YES
        T.Real = YES
      styles/T/Contraction.yml: |
        extends: existence
        message: "Avoid '%s'."
        level: error
        raw:
          - "it['’]s"
      styles/T/Real.yml: |
        extends: existence
        message: "Avoid '%s'."
        level: error
        tokens:
          - real
      test.html: |
        <p>You know it&rsquo;s great software</p>
        <p>It&rsquo;s a real word test</p>
    args: --output=JSON test.html
    exit: 1
    want: |
      {
        "test.html": [
          {
            "Action": {
              "Name": "",
              "Params": null
            },
            "Suggestions": [],
            "Span": [
              13,
              22
            ],
            "Check": "T.Contraction",
            "Description": "",
            "Link": "",
            "Message": "Avoid 'it's'.",
            "Severity": "error",
            "Match": "it's",
            "Line": 1
          },
          {
            "Action": {
              "Name": "",
              "Params": null
            },
            "Suggestions": [],
            "Span": [
              17,
              20
            ],
            "Check": "T.Real",
            "Description": "",
            "Link": "",
            "Message": "Avoid 'real'.",
            "Severity": "error",
            "Match": "real",
            "Line": 2
          }
        ]
      }

  - name: issue-791
    about: "#791 -- a tight list item with a nested list is its own `list`
      block: an end-anchored rule sees the item's ending, not the nested
      child's, and every unpunctuated item is reported at its final word"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        T.ListPunct = YES
      styles/T/ListPunct.yml: |
        extends: existence
        message: "Must put the ending punctuation at the end of a list item."
        nonword: true
        level: error
        scope: list
        tokens:
          - '[^.!?]\s*$'
      test.md: |
        # Testing

        ## Unordered Lists with Different Bullets

        - Item with punctuation.
        + Item without punctuation
        * Another item with punctuation.

        ## Ordered Lists

        1. First ordered item with punctuation.
        2. Second ordered item without punctuation
            - Nested unordered with punctuation.
            + Nested unordered without punctuation
    args: test.md
    exit: 1
    want: |
      test.md:6:26:T.ListPunct:Must put the ending punctuation at the end of a list item.
      test.md:12:42:T.ListPunct:Must put the ending punctuation at the end of a list item.
      test.md:14:42:T.ListPunct:Must put the ending punctuation at the end of a list item.

  - name: nested-markup
    dir: misc/markup
    args: .
    exit: 1
    want: |
      test.md:4:428:Markup.Repetition:"in" is repeated.
      test.md:50:11:Markup.SentSpacing:"d.A" must contain one and only one space.

  - name: inline-dashes
    about: "A dash beside inline markup keeps the source's spacing: none where
      the source had none (#1029), a space where it had one (#1177)."
    dir: misc/dashes
    args: .
    exit: 1
    want: |
      test.md:1:24:Test.Dashes:Remove the spaces around the dash.
      test.md:3:14:Test.Unspaced:Add spaces around 'm—'.
      test.md:5:11:Test.Unspaced:Add spaces around 'm—'.
      test.md:7:12:Test.Unspaced:Add spaces around 's—'.
      test.md:9:5:Test.Dashes:Remove the spaces around the dash.
      test.md:11:5:Test.Dashes:Remove the spaces around the dash.
      test.md:13:5:Test.Dashes:Remove the spaces around the dash.
      test.md:15:5:Test.Dashes:Remove the spaces around the dash.

  - name: spelling
    dir: spelling
    args: .
    requires: asciidoctor, rst2html
    exit: 0
    want: |
      test.dic.md:1:3:Spelling.Test:'Test' is a typo!
      test.html:5:21:Spelling.Ignores:Did you really mean 'docbook'?
      test.html:14:96:Spelling.Ignores:Did you really mean 'TODO'?
      test.md:3:1:Spelling.Ignore:'HTTPie' is a typo!
      test.md:3:96:Spelling.Ignore:'human-friendly' is a typo!
      test.txt:2:1:Spelling.GB:'This' is a typo!
      test.txt:2:6:Spelling.GB:'is' is a typo!
      test.txt:2:9:Spelling.GB:'a' is a typo!

  - name: spelling-v3
    dir: spellingv3
    args: .
    requires: asciidoctor, rst2html
    exit: 1
    want: |
      test.html:5:21:Vale.Spelling:Did you really mean 'docbook'?

  - name: i18n
    dir: i18n
    args: .
    requires: asciidoctor
    exit: 0
    want: |
      ru.adoc:6:12:RU.RUSwap:Возможно, следует написать 'добавить' вместо 'добавлении'?
      ru.adoc:7:12:RU.RUSwap:Возможно, следует написать 'добавить' вместо 'добавления'?
      zh.md:6:1:ZH.Simple:Avoid using "根据"
      zh.md:7:43:ZH.Simple:Avoid using "根据"

  - name: infostrings
    dir: misc/infostring
    args: .
    exit: 1
    want: |
      test.md:16:24:Vale.Spelling:Did you really mean 'Encryptor'?
      test.md:16:35:Vale.Spelling:Did you really mean 'Jasypt'?
      test.md:17:25:Vale.Spelling:Did you really mean 'config'?
      test.md:23:78:Vale.Spelling:Did you really mean 'config'?
      test.md:23:85:Vale.Spelling:Did you really mean 'json'?

  - name: shadowed-by-code-span
    about: a match whose text also appears inside an earlier code span must be
      reported at its prose occurrence, not the span's
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        BasedOnStyles = T
      styles/T/Tok.yml: |
        extends: existence
        message: "'%s' in text"
        level: error
        nonword: true
        raw:
          - ZQX
      test.md: |
        A first paragraph.

        Inline `sum(x) # ZQX` and text ZQX after.

        Both ZQX one and ZQX two.
    args: test.md
    exit: 1
    want: |
      test.md:3:32:T.Tok:'ZQX' in text
      test.md:5:6:T.Tok:'ZQX' in text
      test.md:5:18:T.Tok:'ZQX' in text

  - name: styled-span-line
    about: a block located by a common final word (`it.`) landed on another
      line that happened to hold the same token, and the alert vanished into
      the dedup -- the block's longest word places it instead
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        T.Tok = YES
      styles/T/Tok.yml: |
        extends: existence
        message: "'%s' left in text"
        level: error
        nonword: true
        raw:
          - ZQX
      test.md: |
        A first paragraph in it.

        See **bold prose with a ZQX in it** and *emphasis with a ZQX in it*.

        - A list item with a ZQX in it.
    args: test.md
    exit: 1
    want: |
      test.md:3:25:T.Tok:'ZQX' left in text
      test.md:3:58:T.Tok:'ZQX' left in text
      test.md:5:22:T.Tok:'ZQX' left in text

  - name: json-counts
    about: "--counts adds a per-check tally to JSON output, zeros included: an
      enabled check that never fires is otherwise invisible, so a dead rule is
      indistinguishable from a working one"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        BasedOnStyles = T
      styles/T/Live.yml: |
        extends: existence
        message: "'%s' left in text"
        level: error
        tokens:
          - TODO
      styles/T/Dead.yml: |
        extends: existence
        message: "should never fire"
        level: error
        tokens:
          - zzzz-nothing
      doc.md: |
        A TODO left here.
    args: --output=JSON --counts doc.md
    exit: 1
    want: |
      {
        "files": {
          "doc.md": [
            {
              "Action": {
                "Name": "",
                "Params": null
              },
              "Suggestions": [],
              "Span": [
                3,
                6
              ],
              "Check": "T.Live",
              "Description": "",
              "Link": "",
              "Message": "'TODO' left in text",
              "Severity": "error",
              "Match": "TODO",
              "Line": 1
            }
          ]
        },
        "counts": {
          "T.Dead": 0,
          "T.Live": 1
        }
      }

  - name: deep-indent-directive
    about: "a directive comment on a six-space continuation indent fails
      CommonMark's HTML-block test and lands inline, inside the paragraph; the
      block-level toggle has cancelled out by the time the paragraph is
      linted, so the directive's recorded region suppresses the alert instead"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        BasedOnStyles = T
      styles/T/Hedging.yml: |
        extends: existence
        message: "Hedge: '%s'."
        level: error
        ignorecase: true
        tokens:
          - it's worth noting
      doc.md: |
        - [ ] **Bold lead.** Six-space continuation indent.
              <!-- vale T.Hedging = NO -->
              Here it's worth noting is quoted.
              <!-- vale T.Hedging = YES -->
              And it's worth noting this follows the YES.

        - [ ] Checkbox, two-space indent.
          <!-- vale T.Hedging = NO -->
          Here it's worth noting is quoted.
          <!-- vale T.Hedging = YES -->

        - Plain bullet, six-space indent.
              <!-- vale T.Hedging = NO -->
              Here it's worth noting is quoted.
              <!-- vale T.Hedging = YES -->

        - No directive, so it's worth noting this must fire.
    args: --output=line doc.md
    exit: 1
    want: |
      doc.md:5:11:T.Hedging:Hedge: 'it's worth noting'.
      doc.md:17:20:T.Hedging:Hedge: 'it's worth noting'.

  - name: fix-apply
    about: "`fix --apply` writes every unambiguous fix -- exactly one
      suggestion, at a span no other fix touches -- and reports the rest
      rather than guessing: a multi-suggestion alert is skipped, and so is
      every member of an overlapping pair"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        BasedOnStyles = T
      styles/T/Swap.yml: |
        extends: substitution
        message: "Use '%s'."
        level: warning
        action:
          name: replace
        swap:
          utilize: use
      styles/T/Multi.yml: |
        extends: existence
        message: "Vague: '%s'."
        level: warning
        action:
          name: replace
          params:
            - alpha
            - beta
        tokens:
          - vagueterm
      styles/T/OverA.yml: |
        extends: substitution
        message: "Use '%s'."
        level: warning
        action:
          name: replace
        swap:
          quite bad: poor
      styles/T/OverB.yml: |
        extends: substitution
        message: "Use '%s'."
        level: warning
        action:
          name: replace
        swap:
          bad outcome: failure
      doc.md: |
        We should utilize the API.

        This vagueterm stays, and this was a quite bad outcome.
    args: fix --apply doc.md
    exit: 0
    want: |
      doc.md: applied 1, skipped 3
        3:6	T.Multi	2 suggestions
        3:38	T.OverA	overlaps T.OverB
        3:44	T.OverB	overlaps T.OverA

  - name: occurrence-min-position
    about: "an `occurrence` shortfall with zero matches lands on the scope
      that fell short, anchored to its first word: reported at 1:1, one
      alert also hid every other deficient scope in the file"
    files:
      .vale.ini: |
        StylesPath = styles
        MinAlertLevel = suggestion

        [*.md]
        BasedOnStyles = T
      styles/T/Presence.yml: |
        extends: occurrence
        message: "A paragraph here has no slang (found %d)."
        level: warning
        scope: paragraph
        min: 1
        ignorecase: true
        token: '\b(?:no cap|cooked|rizz)\b'
      doc.md: |
        This rollout is cooked, no cap.

        This paragraph has none of it at all.

        Neither does this one, which is plain corporate prose.
    args: --output=line doc.md
    exit: 0
    want: |
      doc.md:3:1:T.Presence:A paragraph here has no slang (found 0).
      doc.md:5:1:T.Presence:A paragraph here has no slang (found 0).
