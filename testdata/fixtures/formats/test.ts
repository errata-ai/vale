// TODO: add more tests
interface User {
  name: string;
  id: number;
}
 
class UserAccount {
  TODO: string;
  id: number;
 
  constructor(name: string, id: number) {
    this.name = name;
    this.id = id;
  }
}

// XXX: another comment
const XXX: User = new UserAccount("Murphy", 1);