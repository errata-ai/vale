object HelloWorld {
   /* I was not accepted. Maybe it was because I do not have enough experience.
    * This will print 'Hello World' as the output
    */
   def main(args: Array[String]) {
      // Prints Hello World -- no small feat!
      // This is also an example of single line comment.
      println("Hello, world!")
   }
}
